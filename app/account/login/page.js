export const metadata = {
  title: 'Sign In',
  description: 'Sign in to your Robo Tech Valley account to track orders and shop faster.',
};

import LoginForm from './LoginForm';

export default function LoginPage() {
  return <LoginForm />;
}
