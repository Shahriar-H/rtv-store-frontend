/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com',"res.cloudinary.com","via.placeholder.com","store.robotechvalley.com"],
  },
};

module.exports = nextConfig;
